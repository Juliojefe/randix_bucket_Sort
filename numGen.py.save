import random

for i in range(1000):
    # generate a random integer between 0 and 10000
    rand_int = random.randint(0, 10000)

    # add leading zeros until the integer is 9 digits long
    str_int = str(rand_int).zfill(9)

    # print the integer on a new line
    print(str_int)
